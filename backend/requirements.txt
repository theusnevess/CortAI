# --- Core API & Server ---
fastapi==0.133.1
uvicorn[standard]==0.27.1
pydantic==2.7.4
pydantic-settings==2.2.1
python-multipart==0.0.22  # Para upload de arquivos (fix de CVEs)
starlette==0.49.1         # Pin explicito para corrigir CVEs reportadas pelo pip-audit
cryptography==46.0.5      # Pin de seguranca para o stack crypto transitivo
urllib3==2.6.3            # Pin de seguranca para requests/minio
wheel==0.46.2             # Pin de seguranca do tooling local

# --- Database (PostgreSQL) ---
sqlalchemy==2.0.28
alembic==1.13.1           # Migracoes de banco
asyncpg==0.29.0           # Driver Async (Alta performance)
psycopg2-binary==2.9.9    # Driver Sync (Para Celery/Sync tasks)

minio==7.2.4
numpy
librosa
faster-whisper
soundfile

# --- Async Tasks & Queue ---
celery==5.3.6
redis==5.0.1
flower==2.0.1             # Monitoramento do Celery

# --- Security ---
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# --- Video Processing & AI Utils ---
yt-dlp>=2026.2.21         # Download de videos com fix de CVE e extractors recentes
ffmpeg-python==0.2.0      # Wrapper para FFmpeg
moviepy==1.0.3            # Edicao programatica
playwright                # Controla navegadores headless
openai==1.13.3            # Client compativel com Ollama

# --- Utilities ---
httpx==0.27.0             # Cliente HTTP Async (para chamar APIs externas)
python-slugify==8.0.4     # Para limpar nomes de arquivos
loguru==0.7.2             # Logs profissionais
jinja2==3.1.6             # Templates HTML server-side (UI interna)

# --- Test ---
pytest==8.2.2
pytest-cov==5.0.0

# --- Security pins for transitive runtime deps ---
filelock==3.20.3
pillow==12.1.1
protobuf==5.29.6
