# --- Core API & Server ---
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.3
pydantic-settings==2.2.1
python-multipart==0.0.9  # Para upload de arquivos

# --- Database (PostgreSQL) ---
sqlalchemy==2.0.28
alembic==1.13.1          # Migrações de banco
asyncpg==0.29.0          # Driver Async (Alta performance)
psycopg2-binary==2.9.9   # Driver Sync (Para Celery/Sync tasks)

minio==7.2.4
numpy
librosa
faster-whisper
pydantic
soundfile

# --- Async Tasks & Queue ---
celery==5.3.6
redis==5.0.1
flower==2.0.1            # Monitoramento do Celery

# --- Security & Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# --- Video Processing & AI Utils ---
# Use uma versão recente do yt-dlp para pegar correções de extractors e assinatura
yt-dlp[ejs]>=2025.01.01       # Download de vídeos com extras (EJS challenge solver)
ffmpeg-python==0.2.0     # Wrapper para FFmpeg
moviepy==1.0.3           # Edição programática
playwright               # Controla navegadores headless (usado por yt-dlp em páginas JS/Cloudflare)
openai==1.13.3           # Client compatível com Ollama

# --- Utilities ---
httpx==0.27.0            # Cliente HTTP Async (para chamar APIs externas)
python-slugify==8.0.4    # Para limpar nomes de arquivos
loguru==0.7.2            # Logs profissionais

# --- Test ---
pytest==8.2.2
pytest-cov==5.0.0
